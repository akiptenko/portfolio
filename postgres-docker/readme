Демо-приложение, которое скачивает CSV-файл и записывает данные в БД.
Разворачивается через Docker. Состоит из контейнера с БД, скрипта для создания таблиц в БД и Python-скрипта, выполняющего загрузку.
Используемые библиотеки:
import pandas as pd # библиотека Python для обработки и анализа структурированных данных
import psycopg2 # PostgreSQL database adapter for Python
from sqlalchemy import create_engine #The Database Toolkit for Python
import requests #для загрузки CSV файла из интернета
CSV файл взят с сайта https://support.staffbase.com/hc/en-us/articles/360007108391-CSV-File-Examples
Передайте привет тому человеку, который разместил демо-файл на облаке Mail. Часа два пытался оттуда скачать файл.
Сначала нужно нажать кнопку "Скачать", которая не нажимается стандартными библиотеками, затем, распарсив HTML, получить ссылку на загрузку.
По итогу, это не получилось, решил взять демо с другого сайта. Возможно, я слабо умею работать с web.
Делать демо-манипуляции с данными, находить среднее, изменять форматы или что-либо высчитывать не стал, так как это относительно несложно делается.
